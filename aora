#!/usr/bin/env bash
# Vai para a pasta do projeto Aora antes de rodar, para achar o main.py e o .env
PROJECT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" &> /dev/null && pwd)"
cd "$PROJECT_DIR" || exit

python3 main.py "$@"

